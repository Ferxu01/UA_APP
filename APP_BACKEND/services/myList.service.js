const { getQueryResults, generateSelectSqlQuery, generateInsertSqlQuery } = require('../helpers/query.helper');
const conexion = require('../config/db');

const getFavProjects = async () => {
};

const postProjectToFav = async () => {
};

const deleteProjectFromFav = async () => {
};

module.exports = {
    getFavProjects,
    postProjectToFav,
    deleteProjectFromFav,
};